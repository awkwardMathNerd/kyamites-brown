

description: Adafruit CRICKIT shield

compatible: "adafruit,crickit"

include: [i2c-device.yaml, gpio-controller.yaml]

properties:
    label:
      required: true

    "#gpio-cells":
      const: 2

    ngpios:
      required: true
      const: 16